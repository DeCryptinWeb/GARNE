
# getting Messages from client store
query getMessagesState {
    messagesState: messagesState @client {
        messages {
            id
            topic
            text
            type
        }
    }
}
